# Base URL of the discussion WebSocket server, e.g. wss://api.example.com
# The client connects to `${VITE_API_WS_BASE}/ws/projects/{project_id}/discussion`
# using the browser's HttpOnly cookies.
# When unset, an in-memory MOCK transport is used instead (no network).
VITE_API_WS_BASE=
